twig:
    default_path: '%kernel.project_dir%/templates'
    globals:
        pathArticleAttachment: '%upload%'
        link_fb: '%fb%'
        email: '%email.address%'

when@test:
    twig:
        strict_variables: true
